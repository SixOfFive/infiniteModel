# Model test status

Tracks which registered models have been **validated** on this fleet and to what depth. Update this as
new tests run. (Seeded from session testing + decision history; correct any miscategorized rows.)

**Legend**
- ✅ **Fully tested** — loads **and** produces coherent output, validated end-to-end (quant/path noted).
- 🟡 **Partially tested** — a capability is proven, but not full end-to-end, or there's a known caveat.
- ⬜ **Unverified** — registered but not validated here (no recent test evidence).
- ❌ **Not supported** — tested and known NOT to serve on this engine, with the reason recorded. A
  deliberate WON'T-FIX, not a bug to re-open: don't re-investigate without a decision to change it.

| Model | Status | Validated (quant / path) | Notes |
|---|---|---|---|
| `qwen3:4b` | ✅ | int4, bf16 — load + generate | Primary always-resident model; default test load is `int4 ctx=8192 auto`. |
| `qwen2.5:0.5b` | ✅ | int4 (shard-cache serve) — generate | Dense; serve-from-cache validated (gens "Paris"). |
| `qwen2.5:1.5b` | ✅ | bf16 + TP — generate | Used to validate TP mesh keepalive (tp2, gens over long idle). |
| `qwen2.5:7b` | ✅ | int4, bf16 — generate | int4 fused tinygemm ~21 tok/s; TP tested. **int2 (#int2, 2026-07-10): machinery ✅ / quality ❌** — cache compiled + served (`_shards/int2/`, CACHED units at half int4 size), QuantLinear2 installed, Triton w2a16 kernel active + numerically EXACT vs naive (M=1 GEMV zero-diff) — but plain-RTN 2-bit output is token salad (also on 0.5B at g64/g32/g16, MSE-clip, and mixed-tier salvage): needs a GPTQ-class calibrated packer (same format; packer-only follow-up). The int2 live test also flushed out + fixed the #loopback-nexthop routing bug (the real wedge-storm root cause). **int2 GPTQ packer (#38, 2026-07-11): machinery ✅ / quality 🟡** — `gptq_pack.py` calibrated compile (32×512 tokens, GPU subprocess, ~20 min for 7B → 3.93 GB `v2-g64-int2-gptq` cache), stale-v1 auto-invalidation verified live ("packer_hash mismatch — recompile" + load fails loud with the compile hint), served through the unchanged w2a16 path (beast+amdcomp 2-stage pipeline). Quality: salad → grammatical + fact-retrieving ("The capital of France is Paris") but loops on open prose at greedy — the expected GPTQ-2bit profile at 7B scale. Verdict: the tier is now FUNCTIONAL for its stated purpose (capacity: big dense models that can't fit at int4); at ≤7B int4 is strictly better. ⚠ A calibration-scaling A/B (64×1024 tokens) was cut short: beast HARD-CRASHED (abrupt journal end 17:53, reboot; this box's NVIDIA open module has panicked under GPU pressure before) ~4 min into a second GPU compile running beside the reloaded residents — deferred; prefer compiling on an idle box (or CPU) when residents hold the GPU. The informative next test is a 70B int2 compile (~19 GB cache), the tier's actual audience — multi-hour GPU run, operator's call. |
| `qwen2.5:14b` (`-instruct`) | ✅ | bf16, int4 — load + generate | Coexistence + gen-stall watchdog validated (400-tok gen). |
| `qwen-image` (t2i, 20B MMDiT) | ✅ (om3nbox) | **#t2i download/registry/UI ✅** (both controllers; 53.7 GB, t2i badge); **#t2i-serve v1 ✅ — first fleet-served image 2026-07-11 on om3nbox**: `/load?model=qwen-image&quant=int4` (mixed-edge int4 e2, 56/60 blocks, 784/784 fused, build 48–115 s, 12.3 GB GPU + CPU TE = 27.7 GB total) then `POST /v1/images/generations` (sign prompt, seed 42, 20 steps, 1024²) → PNG in 1002 s (~50 s/step under live fleet churn; ~34 clean) with **all sign text EXACT** — matches the gate-test mixed recipe. Survived a concurrent qwen2.5-14b auto-load onto the same node (the contention that killed attempt 1 at step 9/20 via the fwd-watchdog's exit(42); watchdog now defers relaunch while a render is active, `140722b`). **Beast serve ✅ via #t2i-offload (2026-07-11)**: bf16 DiT in RAM + blocks streamed to the 4070TiS per step — 20 steps/1024²/seed 42 in **510 s (~25.5 s/step, faster than om3nbox's GPU-resident int4)**, sign text exact (bf16 reference), 0 GB GPU resident, mid-render VRAM 0.24/15.57 GB, residents coexist, nothing evicted. GPU-resident mode on beast remains edge1-only + evicts all residents — offload is its better mode. | **Offline DiT quant gate test (om3nbox gfx1151, diffusers harness, 2026-07-10, fixed seeds/embeds, 20 steps CFG 4)**: bf16 reference clean (~26 s/step, 38 GB weights); pure RTN int4 g128 (our packer, Triton w4a16 all 840 linears self-checked) = coherent but taxed — text digit/glyph drift ("$6.50"/"WiPi"), grain, lost color grade, 10.8 GB, ~37 s/step; **MIXED (first 2 + last 2 blocks bf16, 56 int4) = near-bf16: all sign text EXACT, grain gone, ~13.5 GB weights (peak 16.8 GB @1024² incl. VAE — beast 4070TiS needs tiled/CPU VAE decode)**. v1 recipe: mixed-edge int4 + Lightning 8-step. Test harness: om3nbox ~/qwen-image-test/. |
| `ministral-3:14b` | ✅ text + **vision** | int4 — load + generate; **vision image→text** | #117 fixed prefill hang (beast→theocomp hop). **Pixtral vision validated END-TO-END (m4c161, 2026-06-29)** via the same Mistral3 path as devstral (chat_template.jinja auto-topped-up on load); image→text answered correctly: "The image contains a red circle and a blue square." |
| `devstral-small-2:24b` | ✅ text + **vision** | int4 — load + generate; **vision image→text** | #83/#89 fixed Mistral3 garbage + immediate-EOS. **Pixtral vision validated END-TO-END (m4c159–161, 2026-06-29)**: split tower (`vision_tower.*` + `multi_modal_projector.*`) materialized from the RAW checkpoint prefixes, 2D rotary rebuilt via the module's own rope-init, `get_image_features(pixel_values, image_sizes)` at the merged 28px grid, embeds spliced at `[IMG]` (id 10) with plain 1D positions. Needed `chat_template.jinja` topped-up into the model dir (Mistral ships it as a separate file, not in tokenizer_config) so the native `<s>[INST][IMG]…[/INST]` renders. image→text answered correctly: "The image contains a red circle and a blue square." |
| `deepseek-r1-distill-llama:70b` | ✅ | int4 — generate | Dense Llama; int4 decode tok/s re-measured (#73). **Detok FIXED (#detok-bytelevel, 2026-07-01):** its `tokenizer_config` declares `tokenizer_class: LlamaTokenizerFast`, so transformers 5.12.1 rebuilt a Llama-2 Metaspace backend that IGNORED the valid ByteLevel `tokenizer.json` → every space rendered as `Ġ`, newlines as `Ċ`, AND the model was fed a spaceless prompt. Fixed in `multimodal._get_tokenizer` (byte-level round-trip probe → verbatim `PreTrainedTokenizerFast` reload). Re-verified live on om3nbox: clean coherent gen to "test". |
| `llama-3.3:70b` | ✅ | int4 — load + short- and long-prompt (4.6k) generate (om3nbox) | Added 2026-07-07 via `unsloth/Llama-3.3-70B-Instruct` (the official `meta-llama` repo is gated and the fleet's HF account isn't on the authorized list). 132 GB bf16 download; **cache-on-first-load built the int4 cache (37.78 GB, 82 units, 80 layers)**; loads from cache in ~92 s, single stage, 80/80 layers GPU (39.2/60.6 GB VRAM w/ KV @ ctx 8192). **Decode 0.61 → 1.73 tok/s (2.8×, m4c187 #dram-dealias, 2026-07-07) → 3.5 tok/s re-benched clean (2026-07-08, addendum below)** — the "4× per-byte gap" was ROOT-CAUSED: the w4a16 split-K GEMV walks qweight along N with a K/2-byte row stride; at these dims (K=8192 → 4096B, a power of two) every row maps to the same DRAM channels, and any matrix over the 32MB MALL collapses (gate/up 28672×8192: **17 GB/s**; the 4MB k/v stayed cached at 235 — why only big-K dense models show it). Fixed by re-allocating packed rows on an ODD multiple of 64B in `prepare_fused` (no kernel change; worst shape 17→175 GB/s) + adding the `BN=64/warps=16` autotune family. Remaining gap to the ~4.5 tok/s pure-kernel ceiling is eager-mode overhead (launch schedule ~362 ms/tok measured vs ~222 ms kernels; HF attention/norms/serve ~215 ms) — graph-decode territory, see ACCELERATION.md. Short prompts coherent on `/api/generate` + `/v1/messages`. **Long prompts FIXED (2026-07-08, 85147c1 `#70b-long-prefill`):** the real killer was the flat 12 s return-conn requeue grace (engine_lifecycle `_grace_doom`, armed by the head's at-prefill return-socket freshen), not `_send` — a dense-70B prefill outlasts any flat timer. The grace now renews in 12 s slices while the owning model reports advancing per-layer forward progress (`fwd_progress_ts` + `last_send_ts`, 120 s quiet window, 1 h hard ceiling — the same liveness basis as `_send`'s adaptive prefill wait). Validated on om3nbox: 1,864-tok prompt (189 s incl. load) and 4,637-tok / 3-chunk prompt (588 s wall, ~7.4 min continuous prefill, ~95 ms/tok) both complete with CORRECT answers; a mid-prefill manual unload still fast-fails in ~1 grace slice (retryable 503). Agent-sized prompts now work on this box — just slow (~1.6 min prefill per 1k tok). **HIP-graph decode tested + eager re-benched (2026-07-08, om3nbox):** `INFINITEMODEL_CUDA_GRAPH=8192` in the im-worker unit (torch 2.12.0a0+rocm7.13.0a20260411, HIP 7.13.60980) — HIP graph capture **and** replay execute without error, but the replayed forward's logits are wrong: self-check rel=0.740 / 0.714 across two independent loads → `[cudagraph] replay self-check vs DynamicCache rel=0.714 -> DISABLED (eager)` both times; the permanent eager fallback worked as designed (coherent gen throughout, no crash). Env var reverted — **no graph win available on this HIP stack; retest on a newer TheRock/HIP**. The same session's clean re-bench corrected the eager number: **3.48–3.52 tok/s (284–287 ms/tok) steady-state** (128-tok runs ×3, two fresh loads, exclusive box, identical with and without the graph flag) — the 1.73 recorded 2026-07-07 was depressed ~2× by contaminated first-token autotune picks under concurrent fleet traffic. Real eager overhead over the ~222 ms/tok kernel floor is ~60 ms/tok (~78% of the ~4.5 tok/s ceiling), not the ~362 ms the launch-schedule sim suggested. StaticCache mirror @ ctx 8192 fit fine (~2.5 GB next to 37.78 GB weights, 60.6 GB unified). **Speculative decode benched (2026-07-08, om3nbox):** registered as a built-in PAIR (draft unsloth/Llama-3.2-1B then 3B; same 128256 vocab), draft auto-attaches on load. Three engine fixes landed en route: draft placed on GPU when free VRAM fits (+4 GB margin; a CPU draft step costs more than the 285 ms verify sweep — spec measured 2.49 vs 3.50 plain), the verify FOLD (bonus token rides the next verify — one target sweep per round instead of two), and a per-gen accept-rate stats line. Result: 1B accept 42% (K=4) = parity; **3B accept 48%/41% (K=4/6) -> spec K=6 3.65 vs 3.46 plain (+5%)**. Bottleneck is now the DRAFT's HF-eager step (~70 ms on this ROCm stack; 7 steps/round ≈ 1.7× the whole 70B sweep) + modest acceptance on technical prose. On NVIDIA/distributed targets the fold should show the real win. NOTE: spec output is greedy-CONSISTENT but not bit-identical to plain (single near-tie argmax flips from batched-verify bf16 numerics; all K variants agree with each other). Spec stays per-request opt-in (options.speculative=true + temperature 0). **Beast-fleet (NVIDIA distributed) spec bench (2026-07-08):** enabled by copying om3nbox's bit-identical int4 shard cache to .38 over Tailscale (40.5 GB rsync, direct path ~5-8 MB/s, ~2.5 h; full sha256 `verify_shard_cache` PASS) — the 132 GB bf16 originals were NOT copied: the model dir carries **sparse header-only placeholder safetensors** (real headers + `truncate` to true size; 132 GB apparent, 352 MB real — do NOT "fix" or read tensor data from them; the verified cache serves every unit, and `_dir_has_model`/`measure_model_weights` only need existence + headers). Load: 2 stages (beast L0-65 GPU+RAM spill, amdcomp L65-80 GPU), ~35-44% GPU-resident. Result: **plain 1.33 tok/s · spec K=4 0.99 (accept 47%) · K=6 1.01 (accept 39%) — spec LOSES ~25%**, same root cause as om3nbox writ larger: the CONTROLLER-side draft ran on CPU (~400 ms/step ≈ 0.5× the ~0.75-1 s distributed sweep; K steps/round ≈ 2× sweep) because the 4070TiS had only 3.7 GB free after greedy GPU-first placement — a 16 GB card can never retain the 3B draft's 6.4+4 GB after the 70B fills it, so **greedy placement and a GPU draft are structurally exclusive on small cards**. The fold itself works (2.78-3.37 tok per single sweep; all outputs BYTE-IDENTICAL to plain on CUDA — no tie-flip here). Spec pays only with a GPU-resident draft: needs draft-aware placement (reserve draft+margin on the controller's GPU at plan time when a registry pair exists) or a smaller draft + smaller margin. **Draft-aware placement built + benched (2026-07-09, `0d03fe4` #draft-gpu):** `/load?draft_gpu=1&draft_margin_gb=` reserves the draft's bf16 size + margin on the controller-co-located GPU node at plan time (+ mirrors the reserve in the per-stage worker budget); `draft_margin_gb` tunes `_load_draft`'s previously-fixed 4 GB cushion. On beast it works exactly as designed — the reserve log fires ("reserving 7.5 GB of controller GPU for the spec draft") and the draft attaches on cuda ("spec draft on GPU (6.0 GB model, 10.2 GB free before)") instead of falling to CPU. But the distributed 2-GPU load (beast 16 GB + amdcomp 12 GB) can't spare 7.5 GB: reserving it forces a bigger shard onto amdcomp, which OOM-drops mid-build every attempt ("no capable nodes left after exclusions") — the 38 GB int4 70B already oversubscribes the fleet's 28 GB of VRAM. So benched **pinned to beast alone** (`node=beast`, ctx 4096, 88% of weights on CPU): **plain 1.00 tok/s · spec K=4 1.03 (accept 52%, 2.91 tok/round) · K=6 1.02 (accept 43%, 3.20 tok/round) — PARITY (+2-3%, noise)**, outputs BYTE-IDENTICAL. Draft-on-GPU ERASED the −25% (CPU-draft penalty gone), but can't net a WIN: with the TARGET 88% on CPU, the batched K+1 verify costs ~(K+1)× a single-token sweep (CPU GEMM scales with batch rows), so the fold's "one sweep verifies K+1 tokens" advantage cancels. **Complete rule across all 3 benches: spec wins only when the TARGET verify is on GPU (batched multi-token forward ~free there) AND the draft is on GPU** — om3nbox +5% (both on GPU, capped by the 70 ms ROCm draft step), beast distributed −25% (CPU draft), beast pinned parity (GPU draft but CPU-bound target). A 70B that doesn't fit the fleet's GPUs cannot benefit from spec regardless of draft placement. Feature is correct + committed; the win needs a card big enough to hold target-mostly-GPU + draft (or a dedicated draft GPU). |
| `mixtral:8x7b` | ✅ | int4 — load + generate; **int4 shard cache** | Fused-3D MoE (per-expert ckpt fused at build); non-fused-compile validated. |
| `olmoe:1b-7b` | ✅ | int4 — load + generate; **int4 shard cache** | Per-expert ckpt fused to 3D; serve-from-cache gens "Paris". |
| `qwen3.6-35b-a3b` | ✅ text + **vision** | int4 — load + generate; **int4 shard cache** (cached==cold); **vision image→text** | qwen3_5_moe hybrid (Gated-DeltaNet). MTP self-spec investigated → not viable (#91). **Vision validated END-TO-END on the existing arch-general path with NO code change** (2026-06-28): `/vision_test` → [100,2048] merged tokens (Qwen3_5MoeVisionPatchMerger, image_token 248056); image→text correctly named shapes + **left/right positions** + colors (red circle / blue square), confirming the interleaved-mRoPE positions (`mrope_section [11,11,10]`, `partial_rotary_factor 0.25`) are correct via `_mrope_position_ids` + the worker's own rotary. 27b-nvfp4 sibling expected to work identically (same path). **Anthropic-path thinking + agent tools fixed (2026-07-07, om3nbox int4):** the model "loaded fine but produced no output" via `/v1/messages` — its template opens `<think>` and it burned the whole budget on reasoning the serve path held back (streams = pings only; non-stream = `''`), and any tool_use/tool_result turn instant-EOS'd because JSON-string `arguments` failed the strict template's mapping iteration, dropping to the flat render. Fixed: the Anthropic `thinking` param now maps onto `enable_thinking` (absent → OFF → direct answers, validated `'Hello there, how are you doing?'`; enabled → real `thinking` blocks/`thinking_delta` stream, validated 377 deltas then the correct answer), and dict-form `arguments` makes the native `<tool_call>`/`<tool_response>` template path work (tool_result turn → `'It is 3:45 PM.'`, no fallback). **MoE expert-row de-alias (m4c188 #dram-dealias, 2026-07-08, om3nbox):** load-time measured choice padded its gate_up expert rows (0.088 → 0.052 ms, 1.7×; down kept unpadded) → decode **15.75 → 16.34 tok/s** clean A/B (same box/prompt/temp-0 text), fused-MoE self-checks ACTIVE. |
| `qwen2.5-omni:7b` | ✅ | distributed multimodal — image+audio→text, speech-out | #22/#35/#36/#37 (vision, audio-in, Talker/token2wav speech-out). **Regression fixed 2026-07-03 (BEAST, transformers 5.12.1):** Omni's checkpoint declares `architectures=["Qwen2_5OmniModel"]` — a bare `*Model` name with no `ForCausalLM`/`ForConditionalGeneration`/`LMHead` marker — which the conservative encoder heuristic in `_spec_from_config` mis-flagged `is_embedding=True`, routing the load to the single-node `AutoModel.from_pretrained` build that transformers rejects (`Qwen2_5OmniConfig` has no `AutoModel` mapping). Composite generative checkpoints (nesting thinker/talker/token2wav/text/vision/audio sub-configs) are now excluded from that heuristic, so Omni takes the pipeline Thinker path. **Re-validated on BEAST:** load OK (28L int4, single stage); `/v1/chat/completions` text OK; `/v1/audio/speech` → valid 24 kHz mono WAV (4.94 s for a one-line prompt). **Speech-out RETIRED (2026-07-15):** the Talker/Token2Wav WAV is intrinsically CHOPPY on this checkpoint — reproduced with HF-native transformers on BOTH 5.12 and 4.52 (params, prompt, and single-shot vocoder all ruled out), so it's the checkpoint, not iM's serve path. Omni deleted from both controllers as the speech engine; `/v1/audio/speech` now defaults to the dedicated **`kokoro`** TTS and only routes to Omni's Talker when a caller names an Omni model. Vision/text path unaffected. |
| `qwen2.5-vl:3b` | ✅ text + **vision** | int4 — load + generate; **vision image→text** | #145 (m4c175+m4c176). Qwen2_5_VL: three fixes vs the generic Qwen path — (1) worker skeleton builds `Qwen2_5_VLTextModel`+lm_head (AutoModelForCausalLM has no `Qwen2_5_VLTextConfig` map); (2) controller image processor falls back to `Qwen2VLImageProcessorPil` (no torchvision); (3) `_mrope3d` flag feeds the rotary 3D `[3,bs,seq]` positions even for text (like Omni). **image→text validated on om3nbox 2026-07-01:** red-circle/blue-square → "a red circle and a blue rectangle" (re-confirmed on m4c176 via `/v1/chat/completions`). Vision works via `/v1/messages`, `/v1/chat/completions`, and `/api/chat` (endpoint gap fixed 64d02bf). |
| `qwen2.5-vl:7b` | ✅ text + **vision** | int4 — load + generate; **vision image→text** | Same arch/code as `qwen2.5-vl:3b`. **image→text confirmed live on om3nbox 2026-07-01, worker m4c176, via `/v1/chat/completions`:** red-circle/blue-square → "The image contains a red circle and a blue square." ⚠ An earlier line-536 rotary crash (`too many indices for tensor of dimension 2`) was on the **m4c175** worker — *before* the `_mrope3d` fix; resolved by m4c176. |
| `qwen2.5-vl:32b` | ✅ text + **vision** | int4 — load + generate; **vision image→text** | Same arch/code. **image→text validated on om3nbox 2026-07-01** (single-node int4 ~18 GB, ctx 4096): "Red Circle in the upper left, Blue Square in the lower right." |
| `nomic-embed-text` | ✅ | embeddings — `/api/embed` + legacy `/api/embeddings` + OpenAI `/v1/embeddings`; **auto-load + idle-unload lifecycle** | Encoder, not a causal-LM (#81). **Full battery re-validated 2026-07-03 on BEAST (9/9):** 768-dim, L2-normed (1.00000), deterministic, batch-of-3, all three endpoints + `usage`, semantic ordering sane (0.822 vs 0.473). **Auto-load wired (b239122) + lifecycle validated 7/7:** a COLD embed request auto-loads the encoder (~2.5 s), `idle_unload_m` reaps it after the idle window, and the next cold request re-loads it — no manual /load needed. **Also on om3nbox (2026-07-03): 10/10 incl. cold auto-load (3.7 s) — and the same text embeds BIT-IDENTICALLY on BEAST/CUDA vs om3nbox/ROCm (cos = 1.000000).** |
| `nvfp4-moe-e2e` (`nm-testing/nvfp4_moe-e2e`) | ✅ | int4 (from nvfp4 source) — **compile + serve-from-cache + generate** | Qwen3-MoE 128-expert per-expert nvfp4 (16.86 GB) → 15.98 GB int4 cache → gens coherently. Validated the per-expert nvfp4 MoE compile fix (m4c132). |
| `qwen2.5-0.5b-gguf` (`Qwen/Qwen2.5-0.5B-Instruct-GGUF`, `q4_k_m`) | ✅ | **GGUF → safetensors normalize** → int4 — load + generate | Validates GGUF ingestion (m4c137–m4c144): `.gguf` dequantized to a safetensors checkpoint at add-time, then served as an ordinary int4 model (gens "France's capital is Paris."). |
| `qwen3.6-27b-nvfp4` | ✅ (text) | int4 (from nvfp4 source) — load + **generate** | Reasoning model (emits `<think>`). Loaded int4 auto (CPU-spilled alongside resident models) + **coherent gen confirmed** (m4c149: "The capital of France is Paris."). Multimodal path untested. |
| `minimax-m2-bf16` | 🟡 | int4 **compile fix proven** (gate clears, packs correctly) | Non-fused per-expert MoE (#119). **Never loaded/generated end-to-end** — CPU-bound/unusable here (<0.3 tok/s, 110 GB int4 vs 33.6 GB fleet VRAM); full compile+load deferred. |
| `qwythos:9b-abliterated` | ✅ | int4 — load + **generate** | qwen3_5; loaded int4 auto (ctx 16384) + **coherent gen confirmed** (m4c149: "The capital of France is Paris."). |
| `qwen2.5-coder:32b` | ✅ | int4 — load + **generate** | Verified 2026-06-30 (BEAST fleet sweep): loaded int4 ctx 16384, "The capital of France is Paris." (CPU-spilled, ~1 tok/s). |
| `nemotron:70b` | ⬜ | — | LOAD-FAIL 2026-06-30: "no capable nodes left after exclusions" after ~17 min of retries (placement/node-flakiness, not coherence — retry on a stable fleet). Note: shares deepseek's Llama-3-vocab + `LlamaTokenizerFast` config, so it was ALSO subject to the `Ġ`/space detok bug — now covered by the same #detok-bytelevel fix (tokenizer round-trip verified clean 2026-07-01); still needs a stable fleet to actually load. |
| `kimi-dev:72b` | ⬜ | — | LOAD-FAIL 2026-06-30: too big for the pool at int4 (needs ~56.9 GB, pool offers 46.8 GB usable; won't fit even at ctx 1024). Needs CPU-spill `distribute` mode, more nodes, or a smaller quant. |
| `kimi-linear-48b-a3b-instruct` | ✅ | int4 — load + **generate** | **Validated 2026-08-09 (om3nbox, gfx1151, pinned `node=InferenceEngine`).** First linear-attention hybrid served: 27 layers = 7 MLA full-attn (0-based {3,7,11,15,19,23,26}) + 20 KDA. int4 25.36 GB, `cpu_frac=0.0`, ctx 8192, `kv_reserved=1.09 GB` (only the 7 full-attn layers grow KV). Coherent on `/api/generate` AND `/v1/chat/completions` (correct linked-list reversal, clean `stop`). **Steady decode ~6.5 tok/s** (12.2/12.3/12.7 s for 80 tok, while co-resident qwen3-30b-a3b served live traffic); **first request after load is ~0.9 tok/s — one-time Triton autotune of the KDA kernels**, then 6x faster. **⚠⚠ Requires `fla-core==0.4.0` — NOT `-U`:** 0.4.1+ change `fused_kda_gate` and die at the first decode step (`unexpected keyword argument 'g_bias'`); load and compile still succeed on the wrong version, so 'it loads' proves nothing. Also needs `triton` on the same box, so a CPU-only controller can never build it (compile on a GPU box). **Cannot run on the iM pool** — KDA kernels are Triton/GPU-only with no CPU fallback and no single iM GPU fits 25.4 GB; the iM copy was deleted 2026-08-09. Crop/rewind features (#prefix-kv resume, #pipefill, spec-decode, MTP, kv_slots>1) are gated OFF — a recurrent state cannot be rewound. Not fused-MoE (`is_moe=False`, 256 per-expert modules), which is why it trails qwen3-30b-a3b's ~28 tok/s despite similar active params — an open optimization, not a defect. See docs/KIMI-LINEAR.md. |
| `coneml-348m-alpha-polish900` | ⬜ | — | Custom small model; not validated here. |
| `gemma-4:12b-it` | ✅ (text + **vision**) | int4 — load + **generate**; vision on bf16 | `gemma4_unified` (no PLE, no KV-sharing). **Text validated (m4c163–164, 2026-06-29):** "The capital of France is Paris…". Needed two worker fixes — materialize Gemma's `embed_scale` buffer (else meta-guard trips at load), and **per-attention-type rotary** (sliding/full interleave: build cos/sin per layer_type + `shared_kv_states={}`, else `None_inv_freq` at gen). **Stop-token FIXED (#stop-eos, 2026-06-30):** `<end_of_turn>` (id 106) is now registered as a stop in `_eos_ids`. **VISION validated (#143, 0b47901, 2026-07-03, om3nbox):** encoder-free `embed_vision` path — pure-torch preprocess (torchvision-free), `get_image_features(pixel_values, image_position_ids)`, boi/eoi-wrapped splice. 7/7: `/vision_test` [256,3840] zero-synthesized; image→text on ALL THREE APIs ("red shape + blue rectangle"); shape/color/position probe (colors+shapes right); **multi-image** attribution exact ("first is red, second is blue", 562 tok = 2×256+wraps); text-only unregressed on the NEWLY-downloaded `chat_template.jinja` (was serving via flat fallback before — template now in the om3nbox model dir). **Bidirectional image-span attention now implemented (2026-07-05):** `use_bidirectional_attention="vision"` is honored — image soft-tokens attend bidirectionally within their block (controller derives runs from the splice positions → `bidir_spans` header → per-stage `_causal_addmask` blockwise overlay; offline mask-parity bit-exact vs the HF reference across all layouts × windows; **shared LM mask path LIVE-validated on 26b** — om3nbox int4 2026-07-05). Audio untested. |
| `gemma-4:31b-it` | ✅ (text + **vision**) | int4 — load + **generate** (multi-stage); **vision encoder** validated | `gemma4` dense (60L, h5376), `gemma4_vision` tower. **Text validated (2026-06-29):** "The capital of France is Paris." — worked on the existing m4c163–164 fixes (embed_scale + per-type rotary) with no new code, including the multi-stage split (num_kv_shared_layers=0 → no cross-shard KV dependency). Stop-token tail FIXED (#stop-eos, 2026-06-30) — same `<end_of_turn>` stop registration as 12B. **VISION validated (tower path, 2026-07-04, BEAST):** the `gemma4` tower variant is now handled (previously CRASHED — `_visual_modules` fell through to the nonexistent `model.model.visual`). New `_visual_modules` branch (`model.vision_tower.` + `model.embed_vision.`, NO key rename) + `_encode_images` `mtype=='gemma4'` branch driving the REAL `Gemma4ImageProcessor` (UNMERGED 768-dim teacher patches + `image_position_ids`; 3×3 pooling is INSIDE the tower, so the 12B unified's pre-merged 6912-dim preprocess is NOT reusable) → `get_image_features(...).pooler_output` [256,5376], LM-ready, boi/eoi-wrapped 1D splice (same serve dict as 12B). **Rotary fix (a3e4813):** `gemma4_vision`'s 1D `inv_freq[18]` was being rebuilt at θ=1e4 but needs **θ=100** (head_dim//2 spatial split); `_materialize_meta_tensors` now rebuilds via the module's own `compute_default_rope_parameters` — `/vision_test` shows `rope_init_fn(module 1D)`, zero missing weights. Qwen2.5-VL rotary UNREGRESSED (still θ=1e4). Commits 1a2cc1c+a3e4813. **Bidirectional image-span attention now honored (2026-07-05, same path as 12B)** — `bidir_spans` reaches every pipeline stage's mask; offline-validated bit-exact vs HF; shared LM mask path LIVE-validated on 26b (om3nbox int4, 2026-07-05), gated on the checkpoint's `use_bidirectional_attention="vision"`. |
| `gpt-oss:20b` | ✅ (text) | **bf16 + int4** — load + **generate** | `gpt_oss` MoE (32 experts, 24L, attention SINKS, interleaved clamped SwiGLU, alternating sliding/full, harmony channels). **MXFP4 normalized→bf16 at ingest** (#161, streaming converter). **bf16 validated (m4c171, 2026-07-01):** eager attn for sinks; "Paris" coherent; harmony final-channel filter (#170) returns only the final answer. **int4 validated (m4c174, 2026-07-01, #166) on om3nbox: 13.06 GB (vs 39 GB bf16, ~3×), "Paris" + 3-primary-colors (streaming) + a correct iterative `fib(n)` with explanation.** int4 recipe: experts are IN-major `[E,hidden,2I]` (`x@W`) so TRANSPOSE→`[E,out,in]` before `_pack4_3d`, run the w4a16 fused kernel with the INTERLEAVED clamped SwiGLU (gate=y[::2],up=y[1::2],clamp±7,(up+1)·gate·σ(1.702·gate)) + per-expert biases; NO eager fallback (transpose-packed) so int4 REQUIRES the fused kernel (ROCm always / CUDA `INFINITEMODEL_CUDA_FUSED_MOE=1`, else clean error → load bf16). Vision/audio untested. Note: coding prompts need a larger `num_predict` (harmony reasoning fills the analysis channel before the final). |
| `gemma-4-26b-a4b-it` | ✅ (text + **vision**) | int4 **shard cache** — load + **generate**; **vision image→text** (int4 on .38-CUDA + bf16 om3nbox) | `gemma4` **MoE (128 experts, 30 layers)** + `gemma4_vision` tower. No PLE/KV-sharing. **Text validated (m4c167–169, 2026-06-29)** via `/api/chat`: "The capital of France is Paris." Needed, beyond the dense 12B/31B fixes (embed_scale + per-type rotary), TWO MoE-specific fixes: **(1)** materialize the router's `scale`/`per_expert_scale` nn.Parameters (init-ones, ABSENT from the checkpoint → meta-guard tripped); **(2)** NEVER int4-quantize the router gate (`Gemma4TextRouter.proj` is a plain nn.Linear → quantizing the 128-expert gate corrupted top-k routing → garbage; excluded in `_quant_scope` + `_quantize_int4_`, cache recompiled). Distributed compile via `/compile_dist` HUNG (worker per-expert pack future never returned) — ⚠ **note carried forward as STALE; re-checked 2026-08-17**: the int4 cache now verifies complete (32/32 files, 15.23 GB, 30 layers) and the model LOADS and GENERATES coherently (3 stages, amdcomp+beast, `/v1/chat/completions` 198 chars, finish=stop). The hang was very likely fixed by `asyncio.wait_for(fut, 1800)` (routes_shards) plus 80745bd #repack-elsewhere; the ORIGINAL `/compile_dist` invocation was NOT re-run, so this records a working cache and a working load, not a reproduction of the compile. Built the cache with the proven local `/compile_shards` instead. Stop-token tail FIXED (#stop-eos, 2026-06-30) — `<end_of_turn>` now a stop. **VISION validated END-TO-END (tower path, 2026-07-04, om3nbox):** shares the 31B `gemma4_vision` tower path (commits 1a2cc1c+a3e4813). BEAST was down, so PULLED to om3nbox (49 GB) — the torchvision-free `Gemma4ImageProcessorPil` resolves there (unlike the 12B unified processor, which hard-requires torchvision). `/vision_test` [256,**2816**] `rope_init_fn(module 1D)` zero-missing; **image→text (bf16):** red background + white circle → *"The center of the image contains a white circle on a red background."* (shape AND color both correct, 293 in / 14 out tok, 14 s). **Re-validated int4 END-TO-END on the production `.38` CUDA fleet (2026-07-05):** distributed serve → *"The center contains a white circle on a red background."* (PASS, 16 s, 293 in / 11 out). ⚠ needed the fleet lean — 26b int4 (15 GB) can't get a stable placement while beast's 16 GB VRAM holds other models (CPU-spill → shard-churn / `no shard`/`worker_net.py` hop-death); once other models were released it placed 2-stage all-GPU. **Bidirectional image-span attention now honored (2026-07-05, same path as 12B)** — offline bit-exact vs HF + **LIVE e2e PASS** (om3nbox int4, 2026-07-05: red-bg/white-circle → *"there is a white circle. The background color is red"*, 256 img tokens spliced, clean end_turn, 292 in / 19 out, 0 errors). **MoE expert-row de-alias (m4c188, 2026-07-08, om3nbox int4):** measured choice kept both expert shapes unpadded (live tensors not aliased — the synthetic bench's 2.9× collapse did not afflict the live allocation); decode ~20.5 tok/s unchanged, `/api/chat` coherent ("The capital of France is Paris.").  **Re-installed + re-validated on om3nbox (2026-09-10, controller 0.3.38 @ fdf5afa):** the model had been removed from that box, so it was re-pulled from `google/gemma-4-26B-A4B-it` (47 GB BF16) and the int4 shard cache rebuilt from scratch by #cache-on-first-load's local `/compile_shards` subprocess — **32/32 files, 15.23 GB, group_size 128, 30 layers**, byte-count identical to the 2026-08-17 cache. Placed **single-stage, all 30 layers on the Strix Halo GPU** (13.86 GB VRAM, **0.0 GB RAM — no CPU spill**), every shard logged `CACHED int4` at ~0.41 GB/layer (the `#cache-case` lowercase-id trap is therefore NOT biting: a bf16 fallthrough would be ~1.0+ GB/layer). Registered under the ORIGINAL-CASE target. **Text PASS** `/v1/chat/completions` → "The capital of France is Paris." (finish=stop); sustained 300-token decode **22.3 tok/s** end-to-end, coherent MoE-routing prose — which also re-confirms the 128-expert router gate stayed bf16 (a quantized gate degenerates to repeated garbage). **Ollama `/api/chat` PASS** → "Jupiter", done_reason=stop. **Vision PASS** (int4, not just bf16): 448×448 red-bg/white-circle → *"The shape in the image is a circle, and the background colour is red"*, 285 in / 18 out, clean stop. |

| `kokoro` (`hexgrad/Kokoro-82M`, tts) | ✅ (both controllers) | **#tts-serve v1 ✅ — dedicated TTS, 2026-07-15** | Kokoro-82M StyleTTS2 speech engine (single co-located worker, ~0.3 GB, 54 voices). `POST /v1/audio/speech {"model":"kokoro","input":…,"voice":"af_heart"}` → clean 24 kHz WAV, validated END-TO-END on BOTH boxes: **beast = CUDA** (~0.25 RTF, ~4× realtime) and **om3nbox = CPU** (~0.5 RTF — its gfx1151 GPU JIT-fails Kokoro's LSTM kernel `MIOpenDropoutHIP.cpp '<utility>' not found`, so the leaf **auto-falls-back to CPU**; 82M params → CPU is fine). Drives Kokoro's `KModel` directly + `misaki.espeak.EspeakFallback` G2P (spacy-free: `kokoro`/`misaki` `--no-deps` + `sys.modules['spacy']` stub + `espeakng-loader`, because blis won't build on py3.13/3.14). **`+ Add model` works** (`hexgrad/Kokoro-82M` pulls config+pth+54 voices — the downloader now fetches `.pth`/`.pt` for no-safetensors repos). OpenAI voice names mapped to Kokoro speakers (`nova→af_nova` …). Media-detail modal shows device/params/weights/sample-rate/voices/last-RTF. Replaces the retired Omni Talker speech path. |
| `qwen3.6-27b` | ✅ text + **vision** + **tools** | int4 — load + generate; int4 shard cache (66/66 files, g128) | Dense `qwen3_5_text`, 64L, 26.9B, ctx 32767. **Smoke-tested 2026-07-21 on om3nbox**: single-node **fully GPU-resident** on InferenceEngine (16.79 GB VRAM, ram 0.0, `cpu_frac=0.0`, 8 GB KV reserved), load 50.3 s, **6.0–7.8 tok/s**. Text ✅ (capital-of-Australia → Canberra); reasoning ✅ (bat-and-ball → **$0.05**, the counterintuitive-correct answer, not the naive $0.10); code ✅ (slicing-free `reverse_string`). **Tools ✅** via `/v1/chat/completions`: `finish_reason=tool_calls`, `get_weather` with `arguments={"city": "Tokyo"}` as a **JSON-STRING** — correct for the OpenAI path (Ollama path returns a dict; see the tool-calling contract). **Vision ✅** — a 96×96 PNG (red circle on white) was correctly described as "a solid red circle on a white background", **even though iM loads this repo as `arch=qwen3_5_text`**: the HF config is `Qwen3_5ForConditionalGeneration` with a `vision_config`/`image_token_id`/`video_token_id`, and the arch-general multimodal path carries it with no code change (same as `qwen3.6-35b-a3b`). Registry advertises `capabilities:["image","video","tools"]` — all three now exercised. ⚠ **`/no_think` is UNRELIABLE on this model**: it emptied the think block on the terse factual + code prompts but was IGNORED on the bat-and-ball prompt (emitted a full "Here's a thinking process:" block and burned the token budget inside it) — budget accordingly, or drive `enable_thinking` explicitly. ⚠ **Verbose but EOS is CORRECT** — left uncapped it drifts into answering the rest of the Cognitive Reflection Test battery (bat/ball → 5-machines-widgets → lily-pads, all three answered correctly) yet still terminates on its own: `done_reason=stop` at 524 tokens against an 800 cap. This is verbosity, **not** a stop-token bug (an earlier 400-token cap made it look like run-away generation). |
| `qwen3.8:27b` | ✅ text + **vision** + **tools** | int4 — load + generate; **no shard cache yet** (loaded with `precompile=0`) | Added 2026-08-14, the day `Qwen/Qwen3.8-27B` was published. **Architecturally IDENTICAL to `qwen3.6-27b`** — same `Qwen3_5ForConditionalGeneration` / `qwen3_5`, 64 layers, hidden 5120, `head_dim` 256, 24/4 heads, vocab 248320, `attn_output_gate`, `partial_rotary_factor` 0.25, interleaved mRoPE `[11,11,10]`, MTP head, vision tower depth 27, and the same hybrid `layer_types` (48 `linear_attention` + 16 `full_attention`, interval 4). It is a retrained checkpoint, not a new arch, so it needed **no architecture work** — 55.59 GB / 18 shards downloaded in ~15 min, then loaded first try. **om3nbox, pinned to InferenceEngine, single stage, 64/64 layers GPU, 16.79 GB VRAM** (the same 16.79 GB as its 3.6 sibling), ctx 8192. **Decode 8.91 tok/s** (112.2 ms/tok, measured by differencing n=32 vs n=128 so prefill cancels — the naive `eval_count/eval_duration` reads 8.86 because `eval_duration` includes prefill). Text ✅ (Canberra); reasoning ✅ (bat-and-ball → **$0.05**, the counterintuitive-correct answer); tools ✅ (`finish_reason=tool_calls`, `{"city": "Tokyo"}` as a JSON **string**, correct for the OpenAI path); vision ✅ (96×96 red circle → "A solid red circle is centered on a plain white background"). Registry auto-advertises `capabilities:["image","video","tools"]` from the vision config. ⚠ **This model exposed a general serving bug** (fixed same day, VERSION 0.3.16): its template opens `<think>`, and `starts_in_think` was only computed when the request sent tools — so on the OpenAI/Ollama endpoints the whole chain-of-thought leaked into `content`, ending in a bare unpaired `</think>`, for every caller that did not define a tool. Not model-specific: it hit every `<think>`-opening model on those two endpoints. Reasoning now lands in `reasoning_content` (OpenAI) / `thinking` (Ollama). ⚠ No int4 shard cache was built — om3nbox's disk is at 99% (35 GB free), and a 27B int4 cache is ~14 GB. Loads therefore re-quantize each time. |

| `laguna-xs-2.1` (`poolside/Laguna-XS-2.1`) | 🟡 | int4 **distributed shard cache** — compile + load ✅; generation **partially degraded** (om3nbox) | `LagunaForCausalLM` (`laguna`), 40 layers (L0 dense + 39 MoE), 62.29 GB bf16, gated attention (`self_attn.g_proj`), per-expert MoE + `shared_expert`. **Two real bugs found and FIXED here, both arch-general:** (1) **`#router-bias-alias` (19c173c)** — every load died with `unmaterialized meta tensor(s) … L1.mlp.gate.e_score_correction_bias …+27 more`. The checkpoint stores the DeepSeek-style router bias at `…mlp.experts.e_score_correction_bias` but the BUILT module tree carries it at `…mlp.gate.…` (transformers remaps it during construction — a rename the streamed build bypasses). `_assign_meta_from_sd` resolves served keys as module attribute paths, found nothing on `mlp.experts`, and silently skipped all 39 non-persistent buffers. Now falls back to a same-scope/same-leaf/same-shape still-meta target. (2) **`#bare-linear-router` (4dc57e6)** — after that, it loaded but looped forever ("The capital of France is Paris." then the same phrase endlessly): its ROUTER is a plain `nn.Linear` named just `gate` under `mlp`, with no `*Router`/`*Gate`-classed ancestor, so the class-name exclusion missed it and **int4 quantized the router**, corrupting top-k expert selection. Exact leaf-name exclusion (`gate`/`router`/`wg`) added to the cache packer AND both cold-load quantizers (int4+int2) so cache == cold load. **Validated after the fix:** `/compile_dist` int4 → 42/42 files, 17.14 GB, 40 layers (~33 min, 2 workers), `/verify_shards` sha-clean; loads in ~33 s single-stage; **short factual generation is CORRECT** ("The capital of France is Paris.", `finish=stop`). ⚠ **RESIDUAL (open):** longer/code output is still degraded — it emits the right answer then fragments, and produced `lambda x: x * 2` instead of `x ** 2`. Profile of leftover int4 RTN damage on a low-active-param MoE, not a crash or routing failure. **Next step if it matters: serve int8 or bf16 to confirm quantization is the cause** (and, if so, look for further precision-sensitive tensors in this novel arch — the gated-attention `g_proj` is the prime suspect). ⚠ A cache built BEFORE 4dc57e6 has the router quantized and must be RECOMPILED — `packer_hash` keys on packer version, not scope, so it will NOT auto-invalidate. |
| `north-mini-code-1.0` (`CohereLabs/North-Mini-Code-1.0`) | ✅ | int4 **distributed shard cache** — compile + load + generate (om3nbox) | `Cohere2MoeForCausalLM` (`cohere2_moe`), 49 layers, 56.78 GB bf16 source. **Compile initially FAILED with `KeyError: 'lm_head.weight'`** — root cause was general, not Cohere-specific: this checkpoint **omits `tie_word_embeddings` from config.json** and ships only `model.embed_tokens.weight` + `model.norm.weight` outside its layers (18730 tensors, no `lm_head.weight`), i.e. it IS tied — but every tied-check read `.get("tie_word_embeddings", False)`, so the compile hunted a head tensor that doesn't exist. (transformers' own `PretrainedConfig` defaults that flag to **True** — the opposite of the old default here, so any checkpoint omitting it was mis-read.) Fixed by `shards._is_tied()`, which asks the **checkpoint** first: no head tensor ⇒ necessarily tied, config only as fallback (`#tied-from-checkpoint`, 2026-08-04). **Validated end-to-end on om3nbox:** `/compile_dist` int4 → **51/51 files, 16.83 GB, 49 layers, ~2 min across 2 workers**, `/verify_shards` sha-clean; cache-backed load 37 s (single stage, all 49L on InferenceEngine, 19.5 GB est / 15.83 GB GPU); `/v1/chat/completions` generated coherently (correctly answered "the capital of France is Paris" and reasoned through the Fibonacci function). NOTE: it's a **chain-of-thought model** — it emits its reasoning preamble ("We need to respond to…") before the answer, so give it a generous `max_tokens` (180 hit `finish_reason=length` mid-answer). Unloaded after the test. |
| `lfm2.5-8b-a1b` (`LiquidAI/LFM2.5-8B-A1B`) | ❌ **NOT SUPPORTED — won't fix (2026-08-03)** | int4 shard cache ✅ compiles + verifies; **load ✗** | **Compiles fine, cannot serve.** `/compile_dist` built the int4 cache across the fleet (24 layers / 11 workers / ~5 min → 26 files, 5.04 GB from 16.9 GB bf16) and `/verify_shards` is sha-clean, but a cache-backed load fails in the worker. **Why: LFM2/LFM2.5 is a HYBRID conv+attention architecture, not a llama-shaped decoder.** Its layers carry `conv.conv` / `conv.in_proj` / `conv.out_proj` + `operator_norm` / `ffn_norm` / `feed_forward.w1-w3`, and HF's `Lfm2MoeModel` exposes **no model-level `rotary_emb`** (rotary is per-attention-layer) — the pipeline reads `model.model.rotary_emb` in ~12 places (`shard_build.py` ×8, `shard_forward.py` ×4). Deeper still, the short-conv operators carry a **conv state across decode steps** that the per-request KV-slot machinery does not model at all, so supporting it is a genuine architecture port (new decode-state plumbing), not a patch. **Operator decision: not worth the effort for one model.** Two real bugs found on the way here WERE fixed and are live, because they affected every architecture: `#header-only-weightmap` (`_weight_map` mmapped the ENTIRE single-file checkpoint just to enumerate tensor names — ENOMEM'd any big single-file model on a RAM-small controller) and `#final-norm-alias` (the final norm was hardcoded as `norm.weight` / `.norm`; LFM2 calls it `embedding_norm` — fixed controller-side in `shards._final_norm_key` and worker-side in `shard_build._final_norm_module` + `worker_load._assemble_sd`). The compiled cache remains valid and reusable if serving support is ever added. |

## How to update
When a model is tested, set its row to ✅/🟡, note the **quant + path** (e.g. `int4 serve-from-cache`,
`bf16 distributed`, `embeddings`) and a one-line evidence note. "Fully tested" requires an actual
**load + coherent generation** (or the model's equivalent output, e.g. embeddings), not just a
successful load or compile. Keep the legend honest — a compiled int4 shard cache alone is 🟡 until a
cache-backed load generates correctly.

Mark a model ❌ only after an actual failed test, and record **why** it can't serve (the architectural
reason, not just the error string) plus the date and that it was a deliberate won't-fix. That row is
what stops a future session from re-deriving the same dead end — and it should also say which fixes
found along the way WERE shipped, so the wasted-effort verdict doesn't get read as "nothing came of it".
